/**
 * Port — contrato para obtener servicios de Auren.
 */
export const IServiceRepository = {
  /**
   * @returns {Promise<Service[]>}
   */
  getAll: async () => { throw new Error('Not implemented') },
}